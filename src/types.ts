export interface PhotoRow {
  mlNumber: string;
  date: string;
  time: string;
  commonName: string;
  latitude: number;
  longitude: number;
}

export interface EnvConfig {
  cookie: string;
  userAgent: string;
}
